# Base10 / Infra

A single-screen, static landing page for Base10's infrastructure initiative and compute grant program.

## Preview locally

Run any static file server from this directory, for example:

```sh
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

The site has no build step or runtime dependencies and can be deployed directly to any static host.
